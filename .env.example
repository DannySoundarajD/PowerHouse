# Sentinel AI Workbench Environment Configuration
# Copy to .env and configure for your system

# ============================================================================
# File Access Configuration
# ============================================================================
# Allow Sentinel to access directories outside the workspace
# Comma-separated list of absolute paths (use forward slashes or escaped backslashes)
# Windows Example: SENTINEL_USER_DIRS="D:/Study Material,C:/Projects,C:/Documents"
# Linux Example: SENTINEL_USER_DIRS="/home/user/Documents,/home/user/Projects"
SENTINEL_USER_DIRS=""

# ============================================================================
# Ollama Configuration
# ============================================================================
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MAX_LOADED_MODELS=2
OLLAMA_NUM_PARALLEL=1

# ============================================================================
# Application Paths
# ============================================================================
SENTINEL_HOME=.
SENTINEL_OUTPUT_DIR=./output
SENTINEL_LOG_DIR=./logs
SENTINEL_DATA_DIR=./data
SENTINEL_KB_DIR=./kb

# ============================================================================
# Logging
# ============================================================================
SENTINEL_LOG_LEVEL=INFO
SENTINEL_LOG_FORMAT=json

# ============================================================================
# Model Configuration
# ============================================================================
# Default router model (qwen3.5:2b or qwen3.5:4b)
SENTINEL_ROUTER_MODEL=qwen3.5:2b

# Context management (Sentinel-style)
SENTINEL_PROTECT_FIRST_N=3
SENTINEL_PROTECT_LAST_N=6
SENTINEL_COMPRESSION_THRESHOLD=0.75

# ============================================================================
# Sandbox Configuration
# ============================================================================
SENTINEL_SANDBOX_MEMORY_LIMIT=2g
SENTINEL_SANDBOX_CPU_LIMIT=1.5
SENTINEL_SANDBOX_TIMEOUT=60

# ============================================================================
# Network Guard
# ============================================================================
SENTINEL_NETWORK_MONITOR_ENABLED=true
SENTINEL_LOOPBACK_ONLY=true

# ============================================================================
# Database
# ============================================================================
SENTINEL_DB_PATH=./data/sessions.db
SENTINEL_DB_WAL_MODE=true

# ============================================================================
# Vector Store (ChromaDB)
# ============================================================================
SENTINEL_CHROMA_PATH=./data/chromadb
SENTINEL_EMBEDDING_MODEL=nomic-embed-text

# ============================================================================
# Development
# ============================================================================
SENTINEL_DEBUG=false
SENTINEL_DEV_MODE=false
